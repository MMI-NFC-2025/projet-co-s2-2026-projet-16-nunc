---
const { title, description } = Astro.props;
---

<header class="w-full px-6 py-10 bg-gradient-to-br from-paprika-c to-yellow-c">
  <div class="max-w-md ml-4">
    <h2 class="text-white-bk">
      {title}
    </h2>

    {description && (
      <small class="block text-white-bk">
        {description}
      </small>
    )}
  </div>
</header>